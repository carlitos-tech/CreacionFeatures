# JWT configuration
JWT_SECRET=replace_with_a_strong_random_secret
JWT_EXPIRES_IN=1h

# Demo credentials (reemplazar con autenticación real en producción)
DEMO_USERNAME=admin
DEMO_PASSWORD=replace_with_a_strong_password

# Server
PORT=3000
